[project]
name = "kioku"
version = "0.1.0"
description = "Kioku v1 — a living memory engine for any LLM, on Cadran virtual hardware"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[tool.pytest.ini_options]
testpaths = ["engine/tests"]
pythonpath = ["."]
addopts = "-q"
